name: wac_task
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  cached_network_image: ^3.3.1
  carousel_slider: ^4.2.1
  dynamic_carousel_indicator: ^1.0.4
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  http: ^1.2.2
  icons_plus: ^5.0.0
  motion_tab_bar: ^2.0.4
  provider: ^6.1.2
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - 'assets/icons/'
